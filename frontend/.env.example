# Desarrollo local: URL completa del backend.
# En Vercel (producción/preview) usar una ruta relativa: VITE_API_URL=/api
# (vercel.json reenvía /api/* al backend en Render, URL hardcodeada ahí)
VITE_API_URL=http://localhost:3000/api
